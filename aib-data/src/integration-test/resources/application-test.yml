logging:
  level:
    org.hibernate.SQL: DEBUG

spring:
  application:
    name: aib-data
  liquibase:
    change-log: classpath:db/changelog/liquibase-changelog.xml

eureka:
  client:
    enabled: false

images:
  pathToFolder: C:\dev\aib-backend\aib-data\src\main\resources\images